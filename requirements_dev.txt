# Development-only tooling, not needed to run or test the integration.
#
# Pinned to the same version as the ruff-pre-commit hook's `rev` in
# .pre-commit-config.yaml, so a local `ruff check` never disagrees with the
# hook or CI. There is no [project] table in pyproject.toml (this repo is a
# Home Assistant custom_component, not an installable package), so this pin
# lives here in requirements_test.txt-style rather than as a `dev` extra.
ruff==0.16.5
